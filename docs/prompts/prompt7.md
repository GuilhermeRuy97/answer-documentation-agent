# ───────────────────────────────────────────────
# FILE 7: agent/state.py
# ───────────────────────────────────────────────
"""
Create `agent/state.py`.

This file defines the LangGraph agent state. No business logic — pure type definitions.

Requirements:
- Import: TypedDict, Annotated, List from typing; BaseMessage from langchain_core.messages; operator

- Define AgentState as TypedDict with these exact fields:
  - session_id: str
  - messages: Annotated[List[BaseMessage], operator.add]
    # Uses add reducer so node returns are appended, not replaced
  - query: str
    # The current user question
  - rewritten_queries: List[str]
    # 2-3 query variants generated by rewrite_query node
  - retrieved_chunks: List[dict]
    # Each dict: {content, source_url, page_title, similarity}
  - relevance_score: float
    # Mean similarity score across retrieved chunks. 0.0 to 1.0
  - answer: str
    # Raw answer text before citation formatting
  - citations: List[dict]
    # Each dict: {title, url, snippet}
  - final_response: str
    # answer + formatted citations block. This is what the API returns.
  - retry_count: int
    # How many times retrieval has been retried. Max = MAX_RETRY_COUNT env var.
"""